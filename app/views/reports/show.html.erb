
<% if @report.report_type == 'sprint-definition' %>
  <%= render 'reports/templates/sprint_definition', locals: {report: @report} %>
<% elsif @report.report_type == 'sprint-release' %>
  <%= render 'reports/templates/sprint_release', locals: {report: @report} %>
<% end %>

<%= @data %>